import { connect } from "react-redux";
import AccountInquiryComponent from './AccountInquiryComponent';


const mapStateToProps = (state /* , props */) => (
    {
    }
  );

const mapDispatchToProps = {
    
  };

export default connect(
    mapStateToProps,
    mapDispatchToProps
  )(AccountInquiryComponent);