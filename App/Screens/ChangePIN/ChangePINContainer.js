import { connect } from "react-redux";
import ChangePINComponent from './ChangePINComponent';

const mapStateToProps = ( /* state , props */) => (
    {
      
    }
  );

const mapDispatchToProps = {
    
  };

export default connect(
    mapStateToProps,
    mapDispatchToProps
  )(ChangePINComponent);