import { connect } from "react-redux";
import ConfirmChangesComponent from './ConfirmChangesComponent';

const mapStateToProps = ( /* state , props */) => (
    {
      
    }
  );

const mapDispatchToProps = {
    
  };

export default connect(
    mapStateToProps,
    mapDispatchToProps
  )(ConfirmChangesComponent);