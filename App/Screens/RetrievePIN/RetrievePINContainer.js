import { connect } from "react-redux";
import RetrievePINComponent from './RetrievePINComponent';

const mapStateToProps = ( /* state , props */) => (
    {
      
    }
  );

const mapDispatchToProps = {
    
  };

export default connect(
    mapStateToProps,
    mapDispatchToProps
  )(RetrievePINComponent);