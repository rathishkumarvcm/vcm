import { connect } from "react-redux";
import RetrievePINInfoComponent from './RetrievePINInfoComponent';

const mapStateToProps = ( /* state , props */) => (
    {
      
    }
  );

const mapDispatchToProps = {
    
  };

export default connect(
    mapStateToProps,
    mapDispatchToProps
  )(RetrievePINInfoComponent);