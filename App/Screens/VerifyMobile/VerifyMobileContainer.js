import { connect } from "react-redux";
import VerifyMobileComponent from './VerifyMobileComponent';

/*----------------------
                                  Redux Methods
                                                             -------------------------- */

const mapStateToProps = (state /* , props */) => (
  {
   
  }
);

const mapDispatchToProps = {
   
};
export default connect(
  mapStateToProps,
  mapDispatchToProps
)(VerifyMobileComponent);